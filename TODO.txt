TODO orthographic cameras:

- ortho picking
- ortho zoom to double click
- navigation cube
- (weights for node refinement for large pointclouds)

TODO clipping:

- polygon clipping (define polygon in screenspace)
  - lock camera when selecting polygon vertices
  - vertices are distorted at the boundaries of the render area
  - picking with ortho cameras
  - TODO: 
  	+ clipping in shader
  	+ render extruded clip polygon
- profile clipping
  - EDL renderer: no depth of volumes stored -> no depth test possible
